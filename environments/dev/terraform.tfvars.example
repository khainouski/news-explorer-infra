region       = "fra1"
droplet_size = "s-1vcpu-2gb-70gb-intel"

ssh_public_key_path = "~/.ssh/id_ed25519.pub"

# Optional — leave commented out to skip DNS for this environment (the default).
# Uncomment to point goskills.xyz at this environment's droplet.
# domain_name = "goskills.xyz"
